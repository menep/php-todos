{
    "name": "pierantonio/php-todos",
    "type": "project",
    "authors": [
        {
            "name": "menep",
            "email": "menep@protonmail.com"
        }
    ],
    "autoload": {
        "classmap": [
            "app/"
        ]
    },
    "require": {
        "vlucas/phpdotenv": "^4.1"
    },
    "scripts": {
        "dev-server": "php -S localhost:8000 -t public",
        "serve": "composer run-script dev-server --timeout=0"
    }
}